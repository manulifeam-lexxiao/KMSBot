# Docker Placeholder

This directory is reserved for Docker assets only.

Allowed future files:

- backend Dockerfile
- frontend Dockerfile
- docker-compose files
- entrypoint scripts

Rules:

- Do not place business logic here.
- Do not hardcode secrets in Dockerfiles or compose files.
- Docker integration belongs to Prompt 12 unless a shared runtime baseline needs a placeholder earlier.
